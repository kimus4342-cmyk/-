diff --git a/.claude/settings.json b/.claude/settings.json
new file mode 100644
index 0000000..e4a6a33
--- /dev/null
+++ b/.claude/settings.json
@@ -0,0 +1,8 @@
+{
+  "permissions": {
+    "allow": [
+      "PowerShell($env:PYTHONUTF8 = \"1\"; $env:PYTHONIOENCODING = \"utf-8\"; .venv\\Scripts\\python main.py)",
+      "PowerShell($env:PYTHONIOENCODING=\"utf-8\"; .\\.venv\\Scripts\\python.exe main.py)"
+    ]
+  }
+}
diff --git a/CLAUDE.md b/CLAUDE.md
new file mode 100644
index 0000000..5c3880c
--- /dev/null
+++ b/CLAUDE.md
@@ -0,0 +1,72 @@
+# BLOOMI — 4050 뷰티 큐레이션 멀티 에이전트
+
+## 프로젝트 개요
+OpenAI API 기반 3단계 파이프라인으로 한국 40-50대 여성 타깃 스킨케어 블로그 글을 자동 생성한다.
+
+**파이프라인 순서:** 리서치 에이전트 → 작성 에이전트 → 검수 에이전트
+
+## 실행 방법
+
+```bash
+# 환경 변수 설정
+cp .env.example .env   # OPENAI_API_KEY 입력
+
+# 의존성 설치
+pip install -r requirements.txt
+
+# 실행
+python main.py
+```
+
+Windows에서는 `run.bat` 사용 가능.
+
+생성된 글은 `bloomi_NNN_주제명_YYYYMMDD_HHMM.md` 형식으로 저장된다.
+
+## 파일 구조
+
+| 파일 | 역할 |
+|------|------|
+| `main.py` | 진입점, 파일 저장 |
+| `pipeline.py` | 3단계 파이프라인 오케스트레이션 |
+| `config.py` | 모델명, 토큰 한도, 시스템 프롬프트 전체 |
+| `models.py` | Pydantic 데이터 모델 (`ResearchOutput`, `ReviewResult`) |
+| `research_agent.py` | ① 리서치 에이전트 |
+| `writing_agent.py` | ② 작성 에이전트 |
+| `review_agent.py` | ③ 검수 에이전트 |
+
+## 모델 설정 (`config.py`)
+
+| 상수 | 기본값 | 용도 |
+|------|--------|------|
+| `RESEARCH_MODEL` | `gpt-4o-mini-search-preview` | 웹 검색 포함 리서치 |
+| `WRITING_MODEL` | `gpt-4o` | 블로그 초안 작성 |
+| `REVIEW_MODEL` | `gpt-4o` | 14항목 체크리스트 검수 |
+
+## 데이터 모델 (`models.py`)
+
+**ResearchOutput** — 리서치 에이전트 출력
+- `topic`: 선정 성분/주제
+- `skin_concern`: 4050 피부 고민
+- `core_message`: 글 전체 핵심 메시지
+- `key_insights`: 임상 인사이트 (700자 이상)
+- `editorial_angle`: 각도 유형 + 선택 이유 + 오프닝 훅
+- `products`: `Product` 리스트 (name, feature, price, url, ingredients)
+
+**ReviewResult** — 검수 에이전트 출력
+- `score`: 1~10 (8 이상 승인, 10~11개 부분 수정, 9개 이하 전면 재작성)
+- `feedback`: 14항목 체크리스트 결과
+- `final_article`: 최종 완성 글
+
+## 시스템 프롬프트 수정 지침
+
+프롬프트는 모두 `config.py`의 `*_SYSTEM_PROMPT` 상수에 집중되어 있다.
+
+- **리서치 프롬프트**: 출력 형식 섹션(`=== TOPIC ===` 등) 변경 시 `research_agent.py`의 파싱 로직도 함께 수정
+- **작성 프롬프트**: 글 구조(① 고정 요소 / ② 중간 섹션) 변경 시 검수 체크리스트(14항목)도 정합성 확인
+- **검수 프롬프트**: 체크리스트 항목 수 변경 시 판정 기준(Yes 12개 이상 등) 수치도 함께 조정
+
+## 주의사항
+
+- `RESEARCH_MODEL`은 `web_search` 도구를 지원하는 모델만 사용 가능 (현재 `gpt-4o-mini-search-preview`)
+- 리서치 프롬프트에서 레티놀은 반복 선정 금지로 명시되어 있음
+- 제품 플레이스홀더(`[정확한 제품명]` 등) 그대로 출력되면 `pipeline.py` 또는 `research_agent.py`의 필터 로직 확인
diff --git a/config.py b/config.py
index 92ec6a9..06926dd 100644
--- a/config.py
+++ b/config.py
@@ -27,7 +27,10 @@ Output EXACTLY in this format — no extra text before or after:
 [주제 성분/제품명, 한국어]
 
 === SKIN_CONCERN ===
-[4050 타깃 피부 고민 2-3가지, 한국어, 쉼표 구분]
+[4050 타깃 피부 고민 3-4가지, 한국어, 쉼표 구분.
+ 선정한 성분과 주제에 맞게 독자의 피부 고민을 구체적으로 세분화할 것.
+ 세분화 기준은 주제에 따라 자유롭게 선택 (연령대·피부 타입·고민 유형 등).
+ 특정 표현(폐경 전/후 등)을 그대로 쓰지 말고 해당 주제에 어울리는 언어로 작성할 것.]
 
 === CORE_MESSAGE ===
 [글 전체가 독자에게 전달할 핵심 메시지 1~3줄.
@@ -39,8 +42,10 @@ Output EXACTLY in this format — no extra text before or after:
 [핵심 임상 인사이트 — 반드시 다음을 포함:
  - 성분의 4050 피부 작용 기전 (생리학적 설명)
  - 논문/연구 기관 인용 (연도 + 핵심 수치 포함), 2개 이상
- - 4050 피부 특성(장벽 약화, 콜라겐 감소, pH 변화)과의 연관성
- 한국어, 600자 이상]
+ - 이 성분과 관련된 4050 피부 특성과의 연관성 (주제에 맞는 특성을 선택할 것, 특정 표현을 고정하지 말 것)
+ - 단독 사용의 한계와 타 성분과의 조합이 더 효과적인 이유 (이 성분에 맞는 실제 조합 근거로 작성, 예시를 그대로 쓰지 말 것)
+ - 독자가 "왜 A 하나만으론 부족한가"를 이해할 수 있는 비교 관점
+ 한국어, 700자 이상]
 
 === EDITORIAL_ANGLE ===
 각도 유형: [혼동 해소형 / 실패 진단형 / 선택 고민형 / 성분 심화형 / 루틴 설계형 / 오해 반박형 중 하나]
@@ -67,7 +72,9 @@ Output EXACTLY in this format — no extra text before or after:
 2. [브랜드명 + 제품명] | [핵심 특징] | [가격] | [구매 링크] | [주요 성분 5가지 이내]
 3. [브랜드명 + 제품명] | [핵심 특징] | [가격] | [구매 링크] | [주요 성분 5가지 이내]
 
-제품 가격대: 입문(~3만원) / 중간(3~7만원) / 프리미엄(7만원+) 순서로 구성.
+제품 구성:
+- 글로벌 브랜드와 국내 브랜드를 적절히 섞어 구성할 것. 특정 브랜드에 편중되지 않도록 한다.
+- 가격대는 저가·중가·고가가 고루 포함되도록 구성하되, 순서는 검색 결과에 따라 자유롭게 선택.
 
 [검색 제한]
 - web_search는 최대 2회 이내로 사용할 것 (주제+논문 1회, 제품 1회)
@@ -82,23 +89,51 @@ WRITING_SYSTEM_PROMPT = """
 독자는 주제에 관심은 있지만 전문가는 아닌 일반인(특히 4050 여성 또는 입문자)입니다.
 리서치 인사이트가 이미 제공되므로 웹 검색 없이 주어진 데이터만으로 글을 작성합니다.
 
+## 예시 사용 원칙
+프롬프트 안의 모든 예시("예:", "예시:", 괄호 안 내용)는 구조와 방향을 이해하기 위한 참고용입니다.
+예시 문장·단어·표현을 그대로 복사해 글에 넣지 마세요. 주제와 리서치 데이터에 맞게 새롭게 작성합니다.
+
+## 인사이트 활용 원칙 (최우선 준수)
+- 리서치 에이전트가 제공한 KEY_INSIGHTS(임상 인사이트)를 글 전체에 반드시 녹여야 합니다.
+- **글의 비중**: 일반 배경 정보(맥락·정의)는 전체의 30% 이하, 리서치 인사이트 기반의 차별화된 내용이 70% 이상을 차지해야 합니다.
+- 일반 정보(예: "펩타이드는 콜라겐 생성을 돕는다")는 맥락을 잡는 용도로만 1~2문장 사용하고, 이후는 반드시 KEY_INSIGHTS에서 가져온 구체적 근거(연구 기관, 수치, 기전)로 이어가야 합니다.
+- 인터넷 검색으로 10초 안에 나오는 수준의 내용만 담긴 섹션은 삭제하거나 반드시 인사이트로 보강합니다.
+
 ## 글쓰기 원칙 (반드시 준수)
 1. 독자의 고민/불편함에서 시작해 해결책으로 흘러가는 구조를 씁니다.
+   결론을 먼저 던지지 말고, 독자가 읽으면서 스스로 답을 찾아가도록 유도합니다.
+   패턴: "왜 이게 헷갈릴까요?" → 공감 → 기준 제시 → "그러면 어떤 선택이 맞을까요?" → 독자 스스로 판단 → 정리.
 2. 단정적 표현("~입니다", "~합니다") 대신 제안형("~로 볼 수 있습니다", "~에 도움이 될 수 있습니다")을 씁니다.
 3. 과학 용어는 바로 다음 문장에서 쉬운 말로 풀이합니다.
-4. 비유는 글 전체에서 2~3개 이하. 반드시 "쉽게 말하면 ~" 태그와 함께, 독자가 직관적으로 이해하기 어려운 개념에만 씁니다.
+4. 비유는 글 전체에서 2~3개 이하. 독자가 직관적으로 이해하기 어려운 개념에만 쓰고, 비유 뒤에는 자연스러운 풀이 문장을 이어 씁니다. 특정 태그("쉽게 말하면" 등) 형식을 고집하지 않습니다.
    - 나쁜 예: 매 단락마다 "~라고 보면 됩니다" 반복
-   - 좋은 예: 활성산소 설명할 때 1회만 → 이후는 직접 설명으로 진행
+   - 좋은 예: 어려운 개념 1회만 비유 사용 → 이후는 직접 설명으로 진행
 5. 볼드(**강조**)는 단락당 1개 이하, 글 전체 2~3개 이하로 제한합니다.
 6. 소제목은 단순 카테고리명이 아니라 핵심 주장이 담긴 문장으로 씁니다.
    - 나쁜 예: "비타민C 성분 설명" / "사용법 안내"
    - 좋은 예: "비타민C가 항산화 세럼의 중심인 이유" / "처음 쓰는 분이 가장 많이 틀리는 사용 순서"
 7. 각 단락의 마지막 문장은 소제목의 주장을 증명한 뒤 한 단계 더 나아간 결론으로 마무리합니다.
    패턴 예: "그래서 결국 ~이 중요합니다" / "~만 볼 것이 아니라 ~까지 함께 봐야 하는 이유가 여기 있습니다"
+   중간 섹션 전체 흐름은 "왜 헷갈리는지 공감 → 판단 기준 제시 → 독자가 스스로 선택할 수 있는 상태"로 마무리할 것.
 8. 마지막 섹션 앞에 "이런 분께 특히 맞습니다"로 대상 분류를 정리합니다.
 9. 한계와 주의사항을 솔직하게 넣어 신뢰감을 높입니다.
    예: "단번에 바꿔주는 마법 같은 제품은 없습니다"
 10. 절대 과장 광고 표현을 쓰지 않습니다.
+11. 글 안에 필자의 목소리를 1~2곳에 자연스럽게 담습니다.
+    형태: "저 역시 처음엔 ~라고 생각했습니다", "많은 분들이 ~라고 물어보시는데요"
+    과도한 사용(매 단락)은 금지. 오프닝 또는 "이런 분께" 섹션 앞뒤에 1회 녹여줍니다.
+12. 각 섹션(소제목 단위)은 최소 5문장 이상 작성합니다. 3문장 이하로 끝나는 섹션은 인사이트나 근거를 추가해 반드시 보강합니다.
+13. KEY_INSIGHTS에 포함된 논문·연구 기관·수치는 글 안에 직접 인용합니다. 막연한 "연구에 따르면"이 아닌 구체적 출처(기관명 또는 연도)를 씁니다.
+
+## 문장 품질 원칙 (반드시 준수)
+1. **군더더기 제거**: '그러나', '하지만', '그런데' 같은 접속사를 불필요하게 반복하지 않습니다. 중복되는 동사나 의미가 겹치는 단어도 다듬습니다.
+2. **중복 의미 삭제**: 한 번 설명한 개념을 문단마다 반복하지 않습니다. 이미 언급한 내용은 짧게 지칭하거나 생략하고 글이 앞으로 나아가게 씁니다.
+3. **주제에 맞는 사례**: 다양한 사례를 넣으려는 욕심보다 주제와 확실히 일치하는 사례 하나를 고릅니다.
+4. **현학적 표현 금지**: 이해하기 어려운 비유나 복잡한 문장은 과감히 삭제하거나 평이하게 다시 씁니다.
+5. **우리말 우선**: 영어 표현, 은어, 신조어는 피하고 우리말을